# 设计依据与技术说明（Design Rationale）

> 这篇讲"为什么这么设计"，`ARCHITECTURE.md` 讲"怎么实现的"，两者互补。
> 改方案（换并行策略 / 换声纹模型 / 调分段长度）前建议先看这篇。

---

## 1. 上游开源项目

本项目是封装服务层，底层依赖以下开源项目：

| 项目 | 在本项目中的作用 | 许可 |
|---|---|---|
| [OpenMOSS/MOSS-Transcribe-Diarize](https://github.com/OpenMOSS/MOSS-Transcribe-Diarize) | 底层 ASR + 说话人分离模型，0.91B 参数，推理核心 | Apache-2.0 |
| [SpeechBrain](https://github.com/speechbrain/speechbrain)（`spkrec-ecapa-voxceleb`） | 声纹 embedding 提取（默认） | Apache-2.0 |
| [Resemblyzer](https://github.com/resemble-ai/Resemblyzer) | 声纹 embedding 提取（备选，兼容旧配置） | Apache-2.0 |
| [scikit-learn](https://github.com/scikit-learn/scikit-learn) | 声纹聚类（层次聚类 + 自适应阈值） | BSD-3-Clause |
| FastAPI / Uvicorn / PyTorch / torchaudio / FFmpeg | Web 服务、推理、音频处理 | 各按各的许可 |

### 声纹模型：为什么默认 ECAPA，不选 Resemblyzer

实测对比的结果。Resemblyzer 作者声明该模型主要面向英语，在本项目的中文会议录音上，不同说话人的向量距离全部挤在 0.87–0.93，几乎无法区分。ECAPA-TDNN（VoxCeleb 上 EER 0.80%）实测不同人距离落在 0.18–0.40，可以区分。

| 维度 | ECAPA-TDNN（默认） | Resemblyzer（备选） |
|---|---|---|
| 架构 | TDNN + 通道注意力 | 3 层 LSTM |
| 向量维度 | 192 | 256 |
| 官方 EER | 0.80% | 约 3.9%–4.5%（第三方复现） |
| 本项目实测 | 跨人距离 0.18–0.40，可区分 | 0.87–0.93，几乎分不开 |

两个模型都跑在 CPU 上（`run_opts={"device": "cpu"}`）。显存是本系统最稀缺的资源，后处理不占用 GPU。Resemblyzer 保留在代码里（`VOICE_MODEL=resemblyzer`）仅作兼容回退。

---

## 2. 为什么必须分段：显存约束

### 2.1 模型标称能力

| 指标 | 数值 |
|---|---|
| 参数量 | 0.91B（908,513,280） |
| 上下文窗口 | 131,072 tokens |
| 音频 token 密度 | 12.5 token / 秒 |
| 90 分钟音频 | 约 67,500 个 audio tokens |
| 官方 RTF | H100 长音频约 0.0197 |

按官方口径，90 分钟音频单遍可处理。

### 2.2 本环境实测上限

22GB 单卡上，单遍稳定处理上限约 10–13 分钟，超过即 OOM。代码中 `MAX_SINGLE_SEC = 810`（13.5 分钟）是留了拼接余量的工程安全值，不是 OOM 临界点。

官方标称 90 分钟与实测 13 分钟不矛盾，限制因素不同：

1. **131K 是上下文容量上限，不是显存工程上限**。前者只取决于 KV cache 能否装下，后者还要算音频编码器激活、临时张量、输出序列、内存碎片。
2. **瓶颈不在 KV cache**。按 114.7 KB/token 算，810 秒音频约 10,125 tokens，KV cache 约 1.2 GB，在 22GB 卡上不是问题。
3. **真正的瓶颈是音频编码器的激活显存**。音频按 Whisper 的 30 秒分块编码，810 秒 = 27 块。整段一次性前向时，编码器各层激活随音频长度线性增长，这才是撑爆 22GB 的主因。
4. **推理栈不同**。官方 90 分钟的成绩在 H100、统一内存、量化 KV cache 条件下取得；本环境是 Turing 架构 + Transformers 原生 `generate()`，未启用量化 KV cache。

### 2.3 分段策略

- **单段总长 ≤ 810 秒**（含重叠）
- **切点锚定到静音点**：用 `ffmpeg silencedetect`（noise=-35dB, d=0.8s）扫描静音，在理想切点附近 ±35% 内找最近静音点，避免把人声从中间切断
- **相邻段保留 30 秒重叠**（`SEG_OVERLAP=30`）：每个切点前后各约 30 秒的音频被转写两次，这段冗余是跨段说话人特征对齐的唯一依据
- **均衡分段**：目标段数 N = ⌈总时长 / 810⌉，按 `per = dur/N` 均分，避免最后一段特别短

---

## 3. 为什么不用张量并行

**双 22GB 不等于 44GB。**

### 3.1 NVLink 不合并显存池

NVLink 解决的是卡间通信带宽，不是显存统一寻址。插上桥接器后，两张卡仍是独立的内存池：

```
torch.cuda.device_count()                        # 2
torch.cuda.get_device_properties(0).total_memory # 22 GB
torch.cuda.get_device_properties(1).total_memory # 22 GB
# 仍是两块独立的 22GB，不存在 44GB
```

要让两张卡承载一个模型 / 一条序列，必须靠张量并行（Tensor Parallelism）这类软件层切分，不是插上桥接器就自动生效。

硬件事实：

- RTX 2080 Ti 仅支持 2 路 NVLink（无 4 路桥接器）
- TU102 两条 x8 链路，单向约 50 GB/s（双向 100 GB/s）；对比 A100 的 600 GB/s、H100 的 900 GB/s，差一个数量级
- Z97 平台 CPU 只有 16 条 PCIe 3.0 通道，双卡通常跑在 x8/x8

### 3.2 张量并行对本项目是负收益

| 反对理由 | 说明 |
|---|---|
| 权重不是瓶颈 | 模型 0.9B，FP16 权重 1.82 GB，单卡绰绰有余。TP 切分权重没有收益 |
| 计算/通信比太差 | TP 每层前向需 2 次 All-Reduce。0.9B 模型单层计算量极小，在约 50 GB/s 链路上，通信延迟成为主导 |
| 推理栈不匹配 | 本项目用 Transformers 原生 `generate()`，上 TP 需换 vLLM / SGLang，改动成本高 |
| 架构代际劣势 | Turing 无原生 BF16、无 FlashAttention-2（见第 6 节），TP 收益进一步打折 |
| 耦合风险 | TP 下两卡强耦合，任一卡异常整个请求失败 |

### 3.3 实际采用：任务并行（段级数据并行）

```
卡 0：独立模型实例 + 独立 asyncio.Lock
卡 1：独立模型实例 + 独立 asyncio.Lock
         ↑
   段队列（贪心装箱：最长段优先投给最空闲的卡）
```

优点：零 GPU 间通信、无同步等待、单卡故障不拖垮整体，不需要额外推理框架。
代价：模型权重在两张卡上各存一份（各 1.82 GB），用显存换实现简单与鲁棒性，对 22GB 卡来说划算。

---

## 4. 为什么需要 CPU 声纹后处理

### 4.1 分段带来的副作用

MOSS 在单次上下文内的说话人一致性很强（AISHELL-4 上 cpCER 14.98%，说话人归属稳定性 Δcp 仅 0.79）。

但切段后：

- 每段是独立推理，段与段之间没有共享上下文
- 上游文档明确：超出上下文窗口后，模型缺乏跨段长程记忆，无法保证分段间的说话人标签一致

表现：第 1 段的 `S01` 和第 2 段的 `S01` 可能不是同一个人。

这是分段的代价，不是模型缺陷。模型本设计为一次看完 90 分钟，为绕开显存限制切段后，跨段对齐需要自行补齐。

### 4.2 后处理方案：保守的全局聚类 + 兜底

全程 CPU 执行，不占 GPU 显存。核心思路：能确定才合并，不确定就维持原样。

```
① 提取 embedding
   对每个"段内说话人"，在它与相邻段的重叠区内取最多 4 个最长语音片段
   → ECAPA-TDNN 提 192 维向量 → L2 归一化

② 全局层次聚类
   所有段的所有片段级向量汇总，做一次 AgglomerativeClustering
   （余弦距离 + average linkage）
   阈值不写死：用 KMeans 对 pairwise 距离分布做二分，取两质心中点 → 自适应

③ 置信度校验
   · 簇内任意两成员距离 > confidence_max_dist（默认 0.9×阈值）→ 判为歧义，整簇不合并
   · 判别力门槛：所有说话人两两距离高度接近（范围 < 0.12 或 max/min < 1.4）
     → 判定声纹本质分不开人 → 放弃聚类

④ 兜底
   未被聚类覆盖的残留，用相邻段重叠区的文本相似度做链式对齐
   衬词防护：段首纯语气词（嗯/哦/对/就是…）去衬词后不足 3 字 → 不参与映射
```

信任优先级：

> 声纹能区分时 → 用声纹结果
> 声纹不能区分时 → 回退到 MOSS 模型原始标签
> 宁可漏合并，绝不错合并。

错合并会把两个人的发言混成一个角色，破坏性远大于漏合并（漏合并只是编号分散）。

### 4.3 为什么用"片段级未平均"向量

早期版本对每个说话人的所有片段 embedding 取平均后聚类，实测出现 `{0:S03→G01, 1:S02→G01}` 这类把不同人误并的情况。

原因：平均会把不同说话人的声纹拉向中心，向量趋同。保留片段级差异后，ECAPA 下不同人距离 0.18–0.40，可区分。

---

## 5. 实测环境与性能

### 5.1 测试平台

| 部件 | 规格 |
|---|---|
| GPU | 2 × RTX 2080 Ti 22GB 魔改显存版，2-Way NVLink 桥接 |
| CPU | Intel i7-4790K（Haswell Refresh，4C/8T） |
| 主板 / 内存 | Z97 芯片组 / 32 GB DDR3 |
| 操作系统 | Ubuntu |

说明：

- NVLink 支持取决于 GPU 型号与 PCB，与显存容量无关，魔改 22GB 版同样可用 NVLink。
- `DEPLOYMENT.md` 写的是标准 11GB 版，那是面向换设备场景的通用部署指南；本环境为 22GB 魔改版，单段上限可放到 810 秒。标准版同样可运行，单段上限小一些。

### 5.2 性能实测

| 指标 | 结果 |
|---|---|
| 单遍处理上限 | 约 810 秒（22GB 单卡） |
| 双卡分段并行 vs 单卡 | 快约 50% |
| 声纹后处理耗时 | CPU 执行，与 GPU 转写流水线重叠 |

### 5.3 为什么是 50% 而不是 100%

理论上限 2×，实测 1.5×，差额来自：

1. **重叠区重复转写**：每个切点前后各 30 秒被算两次，段长约 810 秒时额外音频量约 3–4%
2. **尾部效应与负载不均**：贪心装箱已大幅缓解，但段数为奇数或段长差异大时，仍会出现一卡空转等另一卡
3. **CPU 侧开销**：FFmpeg 切片、声纹提取、文本对齐全在 CPU，i7-4790K + DDR3 是这套配置里最明显的短板
4. **段数放大后处理成本**：显存越紧 → 单段越短 → 段数越多 → 调度与后处理开销占比越高

### 5.4 换新硬件的影响

会更快，但显存仍是刚需。

新架构（Ampere / Ada / Hopper）的收益来源：

- Tensor Core 代际提升 + 原生 BF16
- FlashAttention-2 可用（Turing 用不了）
- 更大显存 → 单段可承载更长音频 → 少切段 → 少后处理
- PCIe 4.0/5.0，卡间带宽不再是瓶颈

注意：

> 算力可以靠并行堆，单段长度只能靠单卡显存堆。

换一张 48GB 卡，收益可能比换两张 24GB 更直接，因为显存直接决定要不要切段，而切段是后面所有复杂度的根源。

---

## 6. 已知边界与优化方向

### 6.1 BF16 精度问题（建议实测）

`moss_server.py` 加载模型使用：

```python
model = AutoModelForCausalLM.from_pretrained(
    MODEL_PATH, trust_remote_code=True,
    dtype=torch.bfloat16,          # ← 注意这里
    attn_implementation=ATTN_IMPL)
```

Turing 架构（sm_75，RTX 20 系）没有原生 BF16 支持，BF16 指令要求 sm_80 及以上。在 2080 Ti 上 BF16 张量可以运行，但矩阵乘法走不了 Tensor Core，只能退化执行。

建议：对比 `dtype=torch.float16`（Turing 有 FP16 Tensor Core），可能显著提速。注意 FP16 动态范围较窄，需观察是否出现数值溢出。

### 6.2 已采用的优化

```python
# moss_server.py
ATTN_IMPL = os.environ.get("MOSS_ATTN_IMPL", "sdpa")
os.environ.setdefault("PYTORCH_CUDA_ALLOC_CONF", "expandable_segments:True")
```

- `attn_implementation="sdpa"`：Turing 上 FlashAttention-2 不可用，SDPA 自动回落到 mem-efficient 后端，注意力显存从 O(n²) 降到 O(n)，这是长音频能跑起来的关键
- `expandable_segments:True`：缓解长时间运行后的显存碎片

### 6.3 可选的进一步优化

| 方向 | 预期收益 | 成本 |
|---|---|---|
| 量化 KV cache（INT8） | 90 分钟 KV 从 7.74 GB 降到 4.11 GB（官方数据） | 低 |
| 改 FP16 替代 BF16 | 可能显著提速（见 6.1） | 低，需验证 |
| 社区版 FlashAttention（Turing SM75 移植，FP16 only） | 注意力进一步加速 | 中，需编译 |
| 换更大显存的单卡 | 单段更长 → 段数减少 → 后处理开销下降 | 硬件成本 |
| 换 Ampere 及以后架构 | BF16 + FA2 + 更强 Tensor Core 全解锁 | 硬件成本 |

### 6.4 其他已知边界

- 部分录音声纹本质难分（音色接近 + 嘈杂）：系统诚实保留模型原始标签，这是设计行为，不是 bug
- MOSS 模型自身跨段标签有时已经一致，此时声纹聚类强行介入反而帮倒忙，判别力门槛就是为此设计的自动开关

---

## 7. 总结

显存不够 → 必须切段 → 切段导致跨段角色失配 → 用 CPU 声纹聚类补回来。

整条链路的每个环节，都是被 22GB 显存这个硬约束逼出来的工程选择。单卡显存更大时，这套复杂度的源头即消失。
